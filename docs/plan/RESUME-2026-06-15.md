# RESUME pointer — read this first after the session restart (2026-06-15)

> ## CURRENT STATE (2026-06-16 ~10:00 — #10 COMPLETE; STOPPED for REPL review)
> HEAD `e3fda9d`. Cold umbrella build GREEN (2948 jobs, 0 warnings); full-library QA sweep
> 0 warnings / 64 files; guarded AxiomAudit covers every headline result + `oseledets_splitting`.
> **request_prompt.md #1–#10 are DONE** (Stage A/B + extensions #1–9 + two-sided splitting #10,
> all 9 phases P0–P8 committed, sorry-free, adversarially audited incl. the headline
> `oseledets_splitting` = faithful+non-vacuous). 
> **DELIBERATE STOP** (user instruction): do NOT start the continuous-flow MET yet. Pending:
> (1) finalize the warm-Lean-REPL accelerator design with the user (being discussed in a fork;
> design = `leancheck` engine on `lean-interact` [warm + `--cold`] + PostToolUse warm hook +
> SubagentStop cold-build gate, all in lean-worker frontmatter, armed via restart; see
> memory `warm-lean-repl-accelerator` + `docs/plan/lean-server-evaluation.md`), then (2) implement
> it, then (3) continuous-flow MET (the user's priority), then (4) final QA. RAM is 31 GiB (NOT 64).
>
> ## (earlier) CURRENT STATE (2026-06-15 ~20:30, #10 in progress — autonomous, ultracode)
> HEAD `8519d26`. Build green. Stage E #1–9 DONE & QA-passed (committed). Now implementing
> **#10 two-sided splitting** off `docs/plan/blueprints/two-sided-met.md` (P0–P8), phase-by-phase
> (implement via lean-worker → adversarial mathematician audit → orchestrator wires root +
> AxiomAudit guards + build + commit). Worker model Opus; workers git-blocked; commit only
> sorry-free phases; each headline lemma → AxiomAudit. Files under `Oseledets/TwoSided/`.
> DONE+committed: P0 Invertible (`162cea3`), P1 SpectralRank + P7 MeasurableInf (`8519d26`).
> IN FLIGHT: P2 StrongExport (agent), P3 KingmanMeans redo (agent — prior died on transient net error).
> REMAINING after P2/P3: P4a (needs P0,P3) → P4b (needs P4a,P2) ∥ P6 (needs P0,P2) → P5 (needs P4b)
> → P8 assembly (`oseledets_splitting` + audit). Dep graph: blueprint §3. Wave scripts:
> `docs/plan/wf-twosided-waveA.js` (resume `wf_b4403348-d96`). THEN: continuous-flow MET (priority),
> final QA. If a phase will not close sorry-free: keep it OUT of root, document the open node, do NOT sorry.
>
> ## (earlier) CURRENT STATE (2026-06-15 ~19:16 — autonomous mode)
> HEAD = `01da598`. Build GREEN. Hooks ARMED (workers git-blocked, verified). Worker model = Opus.
> User directive: run UNATTENDED to completion of everything planned; solve blocks by BUILDING
> missing infra (never commit `sorry`; unfinished deep steps stay OUT of the build as honest open
> nodes); finish #10 fully. Commit per item; every new public result → `Oseledets/AxiomAudit.lean`.
>
> **Stage A** ✅ (linter-clean 0/45, fossils deleted, guarded axiom audit, docs). **Stage B** ✅.
> **Stage E (request_prompt.md) progress:**
> - ✅ #6 Spectrum (`f4cddd0`); #1 sums + #3 sign + telescoping (`ffcd590`); #2 ExteriorCocycle +
>   #7 DetIdentity (`01da598`).
> - ⏳ E3 files VERIFIED-CLEAN on disk but UNCOMMITTED, pending adversarial audit:
>   `Oseledets/Lyapunov/Inverse.lean` (#8), `Restriction.lean` (#5), `NonErgodic.lean` (#9A).
> - ⏳ Workflow `wf_00f8a9fa-56f` (task w2nxhusza, script `docs/plan/wf-stageE-finish.js`) RUNNING:
>   adversarially audits the 3 E3 files + implements&audits #4 `Regularity.lean` (USC) and
>   #9B `Singular.lean` (singular upper bounds). If session restarted: re-verify the E3/new files
>   with the verifier; resume the workflow via `resumeFromRunId: wf_00f8a9fa-56f` (or re-dispatch
>   the unfinished pieces). On completion: review verdicts, fix flagged issues, integrate (root
>   imports + AxiomAudit guards) → full build → commit; then Stage-E QA gate (full linter sweep).
>
> **Then, in order:** #10a discrete two-sided splitting (workflow off `docs/plan/blueprints/two-sided-met.md`,
> P0–P8; build backward cocycle + missing measurability/intersection infra) → **continuous-flow MET
> (RAISED TO PRIORITY — QFT motivation, see memory `request-additive-extensions`)** → final
> comprehensive QA. QFT/modular-time/infinite-dim is OUT OF SCOPE (memory `future-direction-qft-modular-time`).
> Verifier command + integration recipe: see the per-item sections below and the AxiomAudit pattern.

---
(historical, from the restart that armed the hooks:)

The session was restarted to ARM the worker git-block hooks (they only register at
session start). Pick up the unattended Mathlib-candidate mission from here.

## STEP 0 — verify the git-block hooks are now armed (do this FIRST)
Spawn a throwaway `lean-worker` subagent that tries `git status` and `git log` and reports
verbatim. Both MUST be blocked (message from `.claude/hooks/block-git.sh`); a non-git
`echo` must still succeed. Also acceptable: the project hook `.claude/hooks/block-subagent-git.sh`.
- If BLOCKED → good; resume fast PARALLEL workers (workers may never run git; orchestrator does all git).
- If NOT blocked → do NOT trust parallel; fall back to serial-execution + commit-after-each
  (one worker at a time, orchestrator commits each file immediately).

## State at restart
- HEAD = `72de636` (chore(infra): forbid git for worker subagents …). Tree clean. Build GREEN (2934 jobs).
- Worker model = **Opus** (`model: 'opus'`); fable unavailable. Co-author line stays `Claude Fable 5`.
- Stage A **phase-1a COMPLETE** (commits `53905e5` + `abc08fd`): all 11 unverified files cleaned
  (headers, #print-axioms removed, suppressions→omit, lines wrapped, register scrubbed). Build green.
- Stage B (Corollaries) COMPLETE earlier (`3c6077f`). Stage C/D not started.

## Verifier command (per file) — used by all cleanup workers
`lake env lean -DautoImplicit=false -Dlinter.mathlibStandardSet=true -Dlinter.unusedSectionVars=true -Dlinter.unusedVariables=true -Dlinter.style.longFile=1500 <FILE>`
(plain `mathlibStandardSet` alone does NOT flag unusedSectionVars/unusedVariables — keep the explicit flags.)

## Next steps (in order)
1. **Phase-1b** — `docs/plan/wf-stageA-phase1b.js` (25 already-headed smaller files):
   line-wrapping + the few residual warnings the verifier now surfaces. Known residual
   `unusedSectionVars` to fix here: AssemblyFromUpper (2: vslow_subset_lambdaSublevel_of_upper,
   hslowflag_of_upper), LimitEigenbasis (1), ForwardSqueezeCore (1). Then integration build + commit.
2. **Phase-2** — `docs/plan/stageA-phase2-plan.md` (SERIAL, each its own build+commit):
   fossil deletion (6 files, 2406 lines), giant splits (OseledetsLimit 3668 [~60 warnings:
   deprecated toEuclideanLin_apply→toLpLin_apply ×7, tendsto_finset_sum→tendsto_finsetSum ×2,
   ~50 unusedSectionVars, ring-nf, unused vars], Kingman 3412, ExteriorNorm 2711), namespace
   re-homing (Ruelle13→Oseledets.Ruelle, SVDData→Oseledets.SVDChainData, ChainCore→Oseledets.*,
   ExteriorNorm/Weyl during split), ~22 thm + ~8 def renames, central axiom-audit test + CI.
3. **Stage E (NEW, priority)** — implement `request_prompt.md` (repo root): additive
   extensions — positive/neg/nonneg Lyapunov-exponent sums (#1), exterior/k-volume growth
   characterization (#2), sign/vanishing (#3), upper-semicontinuity/regularity in the
   generator (#4, "single most valuable"), restriction to invariant subbundles (#5), full
   spectrum as a consumable object (#6), trace/det = ∫log|det| (#7), inverse/time-reversal
   (#8), non-ergodic + singular-cocycle relaxations (#9). Purely ADDITIVE (don't remove/
   rename/weaken); sorry-free; same toolchain. Blueprint first (some items research-level).
   See memory `request-additive-extensions`.
4. **Stage C (DEMOTED — item #10, lower priority, not blocking)** — two-sided splitting,
   blueprint `docs/plan/blueprints/two-sided-met.md`; and eventually a continuous-time/flow version.
5. **Stage D** — final full QA (general + Mathlib-candidate): verifier clean on ALL files,
   build green, axiom audit of target + Corollaries + new additions = [propext, Classical.choice, Quot.sound].

## Hard rule (the reason for the restart)
Workers must NEVER run git. A phase-1a worker ran `git reset` and wiped 6 siblings' edits.
All worker prompts carry an absolute no-git rule; hooks now enforce it mechanically.
The ORCHESTRATOR performs every git operation.
